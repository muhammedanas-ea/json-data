{
  "name": "json",
  "version": "1.0.0",
  "description": "A JSON server deployment example",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "json-server": "^0.17.0",
    "node-cron": "^3.0.3"
  },
  "engines": {
    "node": ">=14.0.0"
  }
}
